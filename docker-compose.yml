version: '3.8'

services:
  whiteboard-app:
    build: .
    container_name: project-controls-whiteboard
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
